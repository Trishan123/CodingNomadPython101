# When you run the code below, you'll get SyntaxErrors.
# The variable names are all messed up and don't follow the rules!
# Fix the variable names so the code works as expected.

first_variable = 40 #The original error was that it put a 1 with a string which you cannot do
second_variable = 2 # this problem was because the two words S V are seperate so python treats it seperate

final = first_variable + second_variable # I needed to change finally because it is a reserved key word and therefore you cannot make a variable for something that is already a python keyword
print(final) # printing this out because I was able to change it too a variable and now I can get the output 42

